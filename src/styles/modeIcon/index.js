import styled from "@emotion/styled";
import { IconButton } from "@mui/material";

export const ModeIcon = styled(IconButton)(() => ({
  position: "fixed",
  right: "25px",
  top: "77px",
  zIndex: "999999",
  borderRadius: "50%",
  background: "#eee",
  color: "black",
}));
