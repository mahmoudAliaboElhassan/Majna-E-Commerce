// import {   createSelector } from "@reduxjs/toolkit";
 
// const getTotalQuantities = createSelector(
//     (state) => state.cart.items,
//     // count here is state.cart.items
//     // if there is any change in state.cart.items it invoke the function
//     (count) => {
//       const cartQuantities = Object.values(count).reduce(
//         (accumulator, current) => {
//           return accumulator + current;
//         },
//         0
//       );
//       return cartQuantities;
//     }
//   );
  
//   export { getTotalQuantities };